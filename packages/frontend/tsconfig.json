{
  "compilerOptions": {
    "jsx": "react",
    "baseUrl": ".", // This is important for resolving paths relative to the project root
    "paths": {
      "@/*": ["./src/*"] // This maps "@" to the "src" directory
    }
  }
}
